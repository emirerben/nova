# Implementation Plans

Generated by the improve skill on 2026-06-12, from a standard-effort full-repo audit at commit `a49fe589`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Enforce job ownership on generative/music/template status + variant-edit endpoints | P1 | M | — | DONE |
| 002 | Bump Next.js 14.2.3 → latest 14.2.x security patch | P1 | S | — | DONE |
| 003 | Move lyric injection after beat-snap; fix karaoke word-highlight drift | P1 | M | — | DONE |
| 004 | CI dependency caching + one-command `make verify` | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- All four plans are mutually independent and can run in parallel worktrees.
- **003 must land before any other change to `lyric_injector.py` or the lyric consolidation layer** — this bug class has regressed ~10 times; don't interleave lyric work.
- 004's `make verify` is a useful pre-flight for 001 and 003 if it lands first, but is not required.
- Each plan stamps `a49fe589` as its planned-at SHA; run the drift check at the top of each plan before executing.

## Deferred findings (audited, judged real, not planned this round)

- **Tonemap only the footage variants actually use** (TODOS.md "render-speed levers") — largest generative speed win, but TODOS itself says to measure used-vs-total ranges on real jobs before committing; needs a verify-first spike.
- **TS↔Pydantic hand-mirror drift guard** (`music-api.ts` SongSection et al.) — real risk, M effort; candidate for a CI schema-hash check or codegen next round.
- **Parallelize sequential LLM agent calls in `generative_build.py`** — MED-confidence waterfall; investigate-then-fix.
- **Decompose `template_orchestrate.py`** (6,996 lines, all three pipelines flow through it) — real velocity debt, but L effort and MED-HIGH risk; requires characterization tests first. Plan 003's extraction of `compute_snapped_slot_durations` is a small first step in this direction.

## Findings considered and rejected (do not re-audit)

- **Plan routes missing ownership checks** — false; `plan_items.py` uses `CurrentUser` + `plan.user_id` checks throughout.
- **POST /generative-jobs lacks integration tests** — false; `tests/routes/test_generative_jobs.py` and `test_generative_timeline.py` exist.
- **Feature flags fully rolled out but still branching** (`LYRIC_DYNAMIC_CROSSFADE_ENABLED`, `TEXT_RENDERER_SKIA_ENABLED`, etc.) — by design; CLAUDE.md documents them as emergency kill switches with guard tests.
- **Pre-commit hooks missing** — marginal value here (solo dev + agent lint hooks; CI enforces); deliberately not adopted.
- **`chrome-extension://` CSRF allowance in `middleware.ts`** — known limitation, Phase-2 fix planned in the code comment itself.
- **Job-row check-then-act race in Celery transitions** — speculative; the single-claim dispatch pattern holds, no evidenced multi-writer.
- **`str(exc)` detail passthrough in `admin_build_tasks.py:169`** — behind admin auth; trivial; not worth a plan.
- **Empty-base64 handling in `middleware.ts` basic-auth decode** — implicit logic is correct as written; cosmetic.
