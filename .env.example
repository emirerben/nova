# Nova — Environment Variables
# Copy to .env: cp .env.example .env

# ── Storage ────────────────────────────────────────────────────────────────────
# Cloud storage bucket for raw uploads + processed outputs (never git-tracked)
STORAGE_BUCKET=nova-videos-dev
STORAGE_PROVIDER=gcs        # "gcs" or "s3"
# GCS: set GOOGLE_APPLICATION_CREDENTIALS path
# S3: set AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY

# ── Job Queue (Redis) ──────────────────────────────────────────────────────────
# docker-compose default:
REDIS_URL=redis://localhost:6379

# ── Database (PostgreSQL) ──────────────────────────────────────────────────────
# docker-compose default:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nova

# ── AI Models ─────────────────────────────────────────────────────────────────
# For Whisper transcription (scene scoring uses CPU-only models)
OPENAI_API_KEY=sk-...

# Optional: use local faster-whisper instead of OpenAI API
# WHISPER_BACKEND=local       # "local" or "openai-api" (default: openai-api)
# WHISPER_MODEL=base.en       # for local: base.en, small.en, medium.en
