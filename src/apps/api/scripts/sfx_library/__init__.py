"""Reproducible creator sound-effect library (KRI-173).

``synth`` renders procedural effects, ``sources`` fetches pinned CC0
recordings, ``master`` makes every file iPhone-safe and level-matched, and
``catalog`` is the single list of what ships. ``scripts/seed_sfx_library.py``
builds, audits and uploads it.
"""
