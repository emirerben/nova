$segment_instruction

Return a JSON object with these exact fields:
- "transcript": string — full spoken text in the segment
- "hook_text": string — the first compelling sentence that creates curiosity
- "hook_score": float 0–10 — how strongly the opening hooks the viewer
- "best_moments": list of 3–6 objects with {"start_s": float, "end_s": float, "energy": float 0–10, "description": string} covering a VARIETY of durations — include some short moments (3–5s) AND some medium moments (8–12s) AND at least one longer moment (13–20s) so this clip can fill both short and long template slots

Return ONLY valid JSON, no markdown.