/**
 * StyleChip moved to the shared UI layer (src/components/ui/StyleChip.tsx) so
 * the plan flow and the generative instant editor share one copy. This stub
 * keeps existing `./StyleChip` imports working — import from the canonical
 * location for new code.
 */
export { default } from "@/components/ui/StyleChip";
